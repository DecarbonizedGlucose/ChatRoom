#include <iostream>

int main() {
    // std::shared_ptr<thread_pool> pool = std::make_shared<thread_pool>(16);
    // pool->init();
    // std::shared_ptr<TopServer> server = std::make_shared<TopServer>(pool);
    // try {
    //     server->launch();
    // } catch (const std::exception& e) {
    //     std::cerr << "Error launching server: " << e.what() << std::endl;
    //     return 1;
    // }
    return 0;
}